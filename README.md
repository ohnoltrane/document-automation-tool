For CTFd_CSV_downloadall.py
- Download all CSVs from CTFd only lol. 
- If you Inspect page and press Network -> Keep log and press a button or smtg, (record the button press), you can see the Request URL hehehehe

Appendother2LoA_script.py just appends a set of the same PDFs to a different first page. In this case was to add T&Cs to signing documents of multiple different students' first page.
- Now adds all appendix pages to an array and adds all
- Previously Only appends one page per many pages, usual flow (e.g. 3 t&c pages) was to append t&c 1 to all, overwrite the pdfs in the folder with all first pages, so on so forth

doc2pdf_script.py, as it says, just turns all .doc files found in the folder to pdfs
